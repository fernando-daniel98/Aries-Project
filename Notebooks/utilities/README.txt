Os dados do projeto são das fontes:
SIH
SIM
CNES (reconhecido a utilidade para melhor localizar os leitos, mas não é de muita importância)

Para a extração dos dados, utilizou-se o protocolo FTP:
PySUS

Para o processo ETL:
Python

Requirements:
caminho do arquivo: "/home/ferdanielinux/desktop-linux/scripts-python/proj-aires/requirements.txt".
Até então esse documento contém todos os pacotes utilizados, desde a etapa de aprendizado até a etapa de implementação dos scripts.


# Processo de execução

filter -> Pega o dado bruto da área de stage e implementa uma filtragem apenas das variáveis de interesse.
